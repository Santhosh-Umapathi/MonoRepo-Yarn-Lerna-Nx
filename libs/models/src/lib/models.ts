export type TCheck = string | number;

export interface User {
  id: string;
  firstName: string;
  lastName: string;
}

export interface Product {
  id: string;
  title: string;
  description: string;
}
